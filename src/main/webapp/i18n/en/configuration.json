{
    "configuration": {
        "title": "Configuration",
        "route": "Configuration",
        "filter": "Filter (by prefix)",
        "table": {
            "prefix": "Prefix",
            "properties": "Properties"
        }
    }
}
